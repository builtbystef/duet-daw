{
  "name": "duet-sidecar",
  "private": true,
  "type": "module",
  "dependencies": {
    "@earendil-works/pi-agent-core": "0.84.1",
    "@earendil-works/pi-ai": "0.84.1"
  },
  "devDependencies": {
    "@types/bun": "1.3.10",
    "typescript": "5.9.3"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "bun build --compile --target=bun-linux-x64-baseline --minify ./src/main.ts --outfile ./dist/duet-sidecar"
  }
}
